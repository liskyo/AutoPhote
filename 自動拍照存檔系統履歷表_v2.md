# 履歷表專案經歷撰寫建議 - 自動拍照存檔系統 (AutoPhote) v2

以下內容是針對 **104 人力銀行** 履歷表格式優化的專案經歷描述。重點在於將功能性的描述轉化為 **「技術能量」** 與 **「解決問題的能力」**，讓面試官一眼就能看出您對底層運作、效能優化與架構設計的掌握度。

---

## 1. 專案名稱 (Project Title)
**多鏡頭工業檢測影像自動化採集系統 (Multi-Camera Industrial Machine Vision System)**

## 2. 職務角色 (Role)
**Python 自動化軟體工程師 / 系統開發者**

## 3. 專案描述 (Project Description)
> *建議放在 104 的「專案描述」或自傳的「專案成就」欄位*

針對產線品質檢測需求，自主開發的一套高並發影像採集系統。該系統整合 5 台海康威視 (Hikvision) 工業相機，透過 Python 進行硬體底層控制，實現毫秒級的多機同步觸發與自動化歸檔。解決了傳統人工檢測效率低落與影像保存不全的問題，並建立了高容錯的影像傳輸機制，確保生產數據的完整性。

## 4. 關鍵技術 (Key Technologies)
> *請確保這些關鍵字出現在您的技能標籤中*

*   **程式語言**: Python 3.x
*   **GUI 框架**: Tkinter (Responsive Design)
*   **併發處理**: `ThreadPoolExecutor`, `threading` (Producer-Consumer Pattern)
*   **硬體整合**: Hikvision MVS SDK, `ctypes` (DLL Interfacing), Industrial GigE Protocol
*   **資料工程**: ETL Pipeline (Extract-Transform-Load), Fault-Tolerant System Design

## 5. 主要貢獻與技術亮點 (Key Achievements & Contributions)
> *這是履歷中最核心的部分，用列點式呈現您的技術深度*

*   **實現高併發硬體控制 (High-Concurrency Hardware Orchestration)**
    *   運用 `ThreadPoolExecutor` 設計多執行緒架構，同時管理 5 台 GigE 工業相機，將多機觸發延遲控制在 100ms 以內，大幅提升產線檢測節拍 (Takt Time)。
    *   解決了 GUI 主執行緒與硬體 I/O 的阻塞問題，確保系統在高負載拍攝下仍保持 60 FPS 的介面響應速度。

*   **建構高容錯分散式儲存機制 (Fault-Tolerant Distributed Storage)**
    *   設計「本地暫存 + 背景重試」的非同步上傳架構 (Local Buffer with Background Retry)。
    *   即使在工廠網路不穩定或 NAS 斷線的狀況下，系統仍能持續運作並保留數據，待網路恢復後自動回補，達成 **0% 數據遺失率** 的可靠度指標。

*   **底層 SDK 封裝與優化 (SDK Encapsulation & Optimization)**
    *   透過 `ctypes` 深度整合海康威視 MVS C-Style DLL，實作客製化的相機控制邏輯 (如 RGB/Bayer 轉碼、軟體觸發)。
    *   優化影像處理管線，在背景執行緒即時完成 20MP 超高解析度影像的縮圖 (Thumbnailing) 與浮水印疊加，有效降低記憶體峰值 (Peak RAM Usage)。

*   **模組化系統架構設計 (Modular Architecture)**
    *   採用類微服務 (Microservices-like) 的分層架構，將「硬體驅動層」、「業務邏輯層」與「使用者介面層」完全解耦 (Decoupling)。
    *   透過 `settings.json` 實現動態配置管理，讓現場工程師無需修改程式碼即可調整相機 IP 與解析度參數，降低維護成本。

---

## 6. 面試應答模擬 (Interview Prep)
> *當面試官問到此專案時的建議回答方向*

*   **Q: 你在這個專案遇到最大的困難是什麼？**
    *   **A (範例):** 最大的挑戰是處理 5 台高解析度相機同時傳輸產生的大量數據流，這容易導致 UI 卡頓甚至崩潰。我通過將影像擷取 (Capture) 與影像處理 (Processing) 分離到不同的執行緒，並實作生產者-消費者模式 (Producer-Consumer Queue) 來解決這個問題，確保使用者介面永遠流暢。

*   **Q: 為什麼選擇 Python/Tkinter 而不是其他語言？**
    *   **A (範例):** Python 擁有豐富的影像處理庫 (如 PIL/Pillow) 且能透過 ctypes 快速對接硬體 SDK，開發效率極高。雖然 Tkinter 較輕量，但我透過良好的多執行緒設計，證明了它也能負擔工業級的即時監控需求。
