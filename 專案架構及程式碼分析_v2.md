# AutoPhote 工業相機自動化拍攝系統 - 專案架構及程式碼分析 v2

## 1. 專案概述 (Project Overview)
**AutoPhote** 是一個專為工業生產線設計的自動化拍攝與影像上傳系統。本系統整合了海康威視 (Hikvision) 工業相機，支援多台相機同時運作 (預設 5 台)，提供即時監控預覽 (Live Preview)、批次觸發拍摄、自動存檔與非同步上傳功能。

### 核心功能
*   **多相機並行控制**：支援 5 台相機同時觸發，相容 Mock 模擬模式與真實 Hikvision MVS 硬體。
*   **即時預覽 (Live Preview)**：獨立執行緒擷取低解析度影像串流，確保 GUI 操作流暢不延遲。
*   **非同步拍攝與處理**：使用 `ThreadPoolExecutor` 進行併發拍攝，並包含自動縮圖 (Resizing) 與浮水印 (Timestamp Overlay) 處理。
*   **容錯儲存機制**：優先寫入本地暫存區，再由背景服務嘗試上傳至遠端伺服器 (NAS/Server)。若遠端斷線，系統會自動降級並保留本地備份。
*   **動態設定管理**：支援 `settings.json` 熱載入設定，可調整相機 IP、解析度比例及儲存路徑。

---

## 2. 目錄結構 (Directory Structure)

```text
AutoPhote_工業相機/
├── main.py                  # [Entry Point] 程式啟動點，負責 DI 注入與啟動服務
├── config.py                # [Config] 全域設定，負責載入 settings.json 與路徑檢查
├── settings.json            # [Config] 外部設定檔 (IP, 解析度, 路徑等)
├── requirements.txt         # [Dependency] Python 套件依賴
├── system.log               # [Log] 系統運行日誌
├── hardware/                # [Hardware Layer]
│   ├── hik_camera.py        # 真實相機驅動 (Hikvision MVS SDK, ctypes 封裝)
│   └── mock_camera.py       # 模擬相機 (測試用)
├── services/                # [Service Layer]
│   ├── capture_manager.py   # 核心拍攝邏輯 (執行緒池, 預覽串流, 影像處理)
│   ├── upload_manager.py    # 上傳佇列管理 (Producer-Consumer, Retry Logic)
│   └── file_service.py      # 檔案系統操作 (建立目錄, 存檔)
├── ui/                      # [UI Layer]
│   └── dashboard.py         # Tkinter 主介面 (Grid Layout, Canvas Preview)
├── utils/                   # [Utilities]
│   ├── logger.py            # Logging 設定
│   └── image_utils.py       # 影像處理工具 (浮水印)
├── debug_hik.py             # [Tool] 單機連線測試腳本
└── test_camera.py           # [Tool] 相機功能完整測試腳本
```

---

## 3. 核心模組程式碼分析 (Core Code Analysis)

### 3.1 全域設定與路徑管理 (config.py)
*   **動態設定**：優先讀取 `settings.json`，若無則使用預設值。這允許現場工程師在不修改 `py` 檔的情況下調整 `camera_ips` 或 `resize_ratio`。
*   **路徑容錯 (`get_valid_path`)**：系統啟動時會檢查 `REMOTE_SERVER_STORAGE` (通常是 `T:\` 網路磁碟)。若未掛載，會自動切換至專案目錄下的 `Server_Storage` 資料夾，防止程式崩潰。

### 3.2 硬體驅動層 (hardware/hik_camera.py)
此模組封裝了海康威視 MVS Python SDK (透過 `ctypes` 調用 `MvImport` DLL)。
*   **環境依賴**：需安裝 MVS SDK，路徑通常為 `C:\Program Files (x86)\MVS\...`。
*   **連線機制**：透過 `MV_CC_EnumDevices` 掃描 GigE 相機，並根據 `config.py` 中的 IP 表進行匹配連線。
*   **影像擷取 (`grab_image`)**：
    1.  送出軟體觸發指令 (`TriggerSoftware`)。
    2.  取得原始 Bayer 格式資料。
    3.  使用 SDK (`MV_CC_ConvertPixelType`) 轉碼為 RGB8 Packed 格式。
    4.  轉換為 Python `PIL.Image` 物件。
*   **預覽串流 (`start_streaming`)**：
    *   啟動獨立 Daemon Thread (`_preview_loop`)。
    *   持續擷取影像並呼叫 `img.thumbnail((800, 600))` 進行縮圖。
    *   **效能關鍵**：縮圖處理大幅降低了傳送給 UI 主執行緒的資料量，避免 20MP 高解析度原圖卡死介面。

### 3.3 拍攝管理服務 (services/capture_manager.py)
系統的核心控制器，協調硬體與 UI。
*   **執行緒池 (ThreadPoolExecutor)**：
    *   使用 `max_workers=CAMERA_COUNT` 確保所有相機「同時」觸發與處理，而非依序等待。
*   **兩階段拍攝流程**：
    1.  **預覽 (Review)**：`trigger_batch_capture(save_now=False)` -> 影像暫存於記憶體 (`pending_captures`) -> 更新 UI 顯示。
    2.  **存檔 (Save)**：`confirm_save()` -> 從記憶體取出影像 -> 寫入硬碟 -> 加入上傳佇列。
*   **影像後處理**：
    *   **Timestamp Overlay**：在影像左上角繪製相機編號與時間戳記。
    *   **Resizing**：根據 `RESIZE_RATIO` (預設 80%) 使用 `LANCZOS` 演算法縮放影像，平衡畫質與檔案大小。

### 3.4 上傳管理服務 (services/upload_manager.py)
*   **架構**：典型的生產者-消費者 (Producer-Consumer) 模式。
*   **運作機制**：背景執行緒不斷從 `upload_queue` 取出檔案路徑，嘗試將其搬移至遠端路徑。
*   **重試邏輯**：若搬移失敗 (例如網路中斷)，會等待 `Config.UPLOAD_RETRY_DELAY` 秒後重試，最多嘗試 `MAX_RETRIES` 次。若最終失敗，檔案將保留在本地 `temp_buffer`，並記錄 Error Log。

### 3.5 使用者介面 (ui/dashboard.py)
*   **技術**：Python Tkinter。
*   **響應式佈局**：動態計算 Grid 權重，確保 5 個相機預覽視窗能填滿螢幕。
*   **Callback 更新**：透過 `main.py` 注入的回呼函式 (`update_camera_image`) 接收來自硬體執行緒的影像，使用 `Canvas` 繪製更新。
*   **全螢幕檢視**：點擊預覽圖可開啟 `Toplevel` 全螢幕視窗，供作業員詳細檢查瑕疵。

### 3.6 主程式 (main.py)
*   **職責**：依賴注入 (Dependency Injection) 與生命週期管理。
*   **流程**：初始化 Config -> 建立資料夾 -> 啟動 UI -> 啟動 Capture/Upload Managers -> 綁定事件 -> 進入 Main Loop。
*   **關閉處理**：監聽視窗關閉事件，確保釋放相機資源 (`capture_mgr.shutdown`) 與停止執行緒。

---

## 4. 關鍵運作資料流 (Data Flow)

### 4.1 即時預覽迴圈 (Live Preview Loop)
1.  **Thread**: `HikCamera._preview_loop` 持續運行。
2.  **Acquire**: 呼叫 SDK 取得最新 Frame。
3.  **Process**: 轉碼 RGB -> `thumbnail` 縮小至 800px 寬。
4.  **Callback**: 觸發 `CaptureManager` -> `Main` -> `DashboardApp`。
5.  **Render**: UI 執行緒更新 Canvas 內容。

### 4.2 拍照存檔流程 (Capture & Save)
1.  **User Action**: 使用者按下「空白鍵 Space」。
2.  **Trigger**: `CaptureManager` 提交 5 個 Task 給 `Executor`。
3.  **H/W Capture**: 相機擷取全解析度 (High-Res) 原始影像。
4.  **Processing**: 
    *   疊加時間浮水印。
    *   暫存於 RAM (`pending_captures`)。
5.  **Review**: UI 顯示靜態截圖 (此時相機狀態變為 Reviewing)。
6.  **User Confirm**: 使用者按下「Enter 鍵」。
7.  **Save**: 
    *   執行 `LANCZOS` 縮放到指定 `RESIZE_RATIO`。
    *   存入本地 `temp_buffer` (JPG High Quality)。
8.  **Upload**: 路徑放入 Queue -> `UploadManager` 搬移至 NAS。

---

## 5. 開發與除錯工具 (Dev Tools)

專案內附帶了幾個重要的測試腳本，位於根目錄：
*   **`debug_hik.py`**：
    *   用途：最小化測試腳本。用於測試單台相機是否能透過 SDK 成功連線並取得影像，不涉及 UI。
    *   情境：當主程式無法連線相機時，先執行此腳本釐清是硬體問題還是程式邏輯問題。
*   **`test_camera.py`**：
    *   用途：完整單元測試。測試 `HikCamera` 類別的封裝，包含連線、斷線、連續擷取與異常處理。
*   **`debug_startup.py`**：
    *   用途：啟動環境檢查。確認 Python 路徑與依賴套件是否正確。

---

## 6. 未來優化建議 (Future Improvements)

1.  **日誌輪替 (Log Rotation)**：目前 `system.log` 無上限增長，建議引入 `Logging.handlers.RotatingFileHandler` 限制檔案大小。
2.  **斷線自動重連**：目前若運行中斷線，需重啟軟體。可在 `HikCamera` 增加心跳偵測 (Heartbeat) 與自動重連機制。
3.  **UI 效能優化**：目前的預覽更新頻率依賴 `time.sleep` 控制，未來可改用 `FPS` 鎖定機制或依賴 UI 繪圖完成訊號來動態調整。
4.  **設定介面**：開發一個簡易的「設定視窗」，讓使用者能直接在 UI 修改 IP 或延遲參數，無需編輯 JSON 檔。
